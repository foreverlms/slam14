{
    "tasks": [
        {
            "type": "shell",
            "label": "make build",
            "command": "./build.sh",
            "options": {
                // "cwd": "{workspaceFolder}/"
            }
        }
    ],
    "version": "2.0.0"
}